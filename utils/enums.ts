export enum PAGE_URL {
  HOME = '/',
  AUTH_LOGIN = '/auth/login',
  USER_SETTINGS = '/user/settings',
}

export enum PAGE_NAME {
  HOME = 'home',
  AUTH_LOGIN = 'auth-login',
  USER_SETTINGS = 'user-settings',
}
